<snippet>
	<content><![CDATA[
${2:private} \$${1:name};

${3:private} function set_${1:name}(\$new_${1:name})
{
	\$this->${1:name} = \$new_${1:name};
	return \$this;
}
${4:private} function get_${1:name}()
{
	return \$this->${1:name};
}
]]></content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>privatevarphpclass</tabTrigger>
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<!-- <scope>source.python</scope> -->
</snippet>
